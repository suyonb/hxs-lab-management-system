# 从权限底座到 3D 实验室：HXS 实验室管理系统项目介绍

实验室日常管理看似只是记录设备、试剂和实验，真正运行起来却会遇到许多细碎但关键的问题：仪器预约是否冲突、耗材领用后库存是否同步、审批记录能否追溯、实验过程是否完整留档，以及管理人员能否快速了解每个实验室的实时状态。

基于这些实际场景，我开发了 **HXS 实验室管理系统**。它不是单纯的数据录入后台，而是一套覆盖实验室基础资料、仪器设备、试剂耗材、实验任务、统一审批和空间可视化的前后端分离管理平台。

目前项目一期从阶段 0 到阶段 6 均已完成，并通过本地 Oracle 环境的业务流程验收。

## 项目解决什么问题

传统实验室管理常依赖 Excel、纸质记录或多个互不关联的系统，容易出现以下问题：

- 仪器状态、预约计划和维修记录相互割裂
- 试剂耗材只有库存数字，缺少批次、有效期和领用流水
- 审批分散在不同页面，申请人和审批人难以统一跟踪
- 实验记录、附件、仪器使用和物资领用无法形成完整链路
- 权限粒度不足，不同实验室、角色和用户看到的数据范围难以控制
- 实验室位置、房间和设备只能通过表格查看，缺少直观的空间表达

HXS 实验室管理系统围绕这些问题，将实验室业务拆分为多个相互关联的闭环，让数据从“录入后可查询”进一步变成“过程可控制、结果可追溯、状态可展示”。

## 系统整体架构

项目采用前后端分离架构：

```text
Vue 3 + TypeScript + Vite + Ant Design Vue
                    |
              RESTful API
                    |
       ASP.NET Core .NET 9 Web API
                    |
                  Oracle
```

前端负责动态菜单、权限控制、业务页面和 2D/3D 可视化；后端负责身份认证、数据权限、业务状态机、事务处理、文件安全和操作审计；Oracle 保存系统配置及实验室业务数据。

## 六个核心业务阶段

### 1. 系统权限底座

系统实现了 JWT 登录认证、用户与角色管理、动态菜单、按钮权限、数据范围和操作审计。菜单及组件路径由数据库动态控制，管理员新增页面菜单后，不需要再到前端维护一份固定路由映射。

权限不仅决定“能不能看见按钮”，还会在后端接口再次校验。系统管理员、实验管理员和普通实验员拥有不同的菜单权限、操作权限及数据范围，未登录、无操作权限和越权访问会分别得到明确的响应。

### 2. 实验室基础数据

基础数据模块维护实验室、楼栋楼层与房间位置、课题组及成员、供应商和数据字典。这些数据不是孤立的档案，而是后续仪器、库存、实验任务和空间可视化的共同基础。

### 3. 仪器设备业务闭环

仪器模块覆盖台账、预约申请、预约审批、使用登记、设备报修和维修处理。

系统会校验预约时间是否冲突；预约通过后可以关联实际使用记录；设备报修审批后自动进入维修状态，维修完成后恢复正常状态。整个流程包含状态机、数据范围、重复审批拦截和审计记录。

### 4. 试剂耗材库存闭环

库存模块包含物资档案、批次入库、库存调整、领用申请、领用审批、库存流水和预警。

领用审批在数据库事务中完成，并按有效期优先扣减可用批次。系统会阻止负库存、过期批次领用和重复审批，同时提供低库存、临期和过期预警。每次入库、调整和领用都会留下可追溯流水。

### 5. 实验任务与电子实验记录

实验任务支持草稿、进行中、已完成和已归档等状态。任务可以关联已通过的仪器预约和物资领用申请，也可以持续补充实验过程、结果、原始数据说明及附件。

归档后的实验默认只读，普通实验员只能操作本人实验，实验管理员可在授权范围内查询，系统管理员拥有受控的解档能力。这样可以把“做了什么实验、使用了什么设备和物资、最终结果是什么”串成完整记录。

### 6. 统一审批、统计与空间可视化

统一审批中心聚合仪器预约、试剂领用和设备报修，集中展示我的申请、待我审批和已审批事项。首页则从实验室业务出发，展示预约、维修、库存预警、实验任务和趋势统计，并支持相关业务数据的 Excel 导出。

空间模块提供 2D 与 3D 两种查看方式。用户可以按实验室和楼层查看房间，进入对应房间后查看仪器信息，也可以搜索设备、筛选状态、聚焦楼层、复位视角和全屏浏览。空间节点会联动仪器运行状态、维修情况、待执行预约和最近预约时间。

系统还提供 3D 场景管理，可维护场景、节点坐标、缩放、排序及实验室、位置、仪器之间的业务绑定，并支持 GLB 模型上传和版本切换。

## 一个辅助亮点：AI 数据推理

系统内置了数据推理会话页面，用户可以输入实验或业务描述，由系统提取结构化信息并返回推理结果。当前功能定位是实验室业务的辅助入口，为后续接入大模型、知识库、实验数据分析和智能预警预留扩展空间。

## 我认为项目最值得关注的设计

**第一，菜单和权限真正动态化。** 页面菜单、组件路径和操作权限由后台配置驱动，角色授权后即可形成不同的系统入口，不需要每次增加菜单都修改前端映射代码。

**第二，核心业务采用闭环设计。** 项目不只提供增删改查，而是通过预约、审批、使用、维修、入库、领用、记录和归档等状态流转，让每一步都有前置条件和结果反馈。

**第三，库存操作强调一致性。** 领用审批采用事务和锁机制，库存不足或处理中任一步失败时整体回滚，避免重复扣减及数据不一致。

**第四，空间展示与真实业务数据联动。** 3D 页面并非静态模型浏览器，房间和仪器节点会同步展示预约、维修及运行状态；当设备状态变化时，空间视图也能反映对应信息。

**第五，提供无后端在线演示模式。** 为了方便通过文章或 GitHub 展示项目，前端内置了浏览器演示数据，不需要部署 API 和 Oracle 也能体验登录、列表操作、审批、数据推理和 2D/3D 空间功能。演示操作仅保存在当前浏览器，并可随时恢复初始数据。

## 当前完成度

项目一期已经完成以下验证：

- 阶段 0 至阶段 6 的代码、数据库迁移、菜单权限和业务流程均已完成
- 后端自动化测试、解决方案构建、前端类型检查及生产构建通过
- 仪器预约、库存领用、实验记录、统一审批等核心流程已在本地 Oracle 环境验收
- 桌面端和移动端布局、2D/3D 切换、全屏及深浅主题完成浏览器检查

当前版本适合作为实验室管理系统原型、毕业设计、前后端分离项目案例或后续行业化开发的基础。正式生产部署时，还需要根据目标环境配置独立密钥、数据库与附件备份、日志保留策略，并替换为真实实验室 GLB 场景素材。

## 在线体验与源码

- GitHub 源码：[HXS 实验室管理系统](https://github.com/suyonb/hxs-lab-management-system)
- 在线演示：[GitHub Pages 演示版](https://suyonb.github.io/hxs-lab-management-system/)

在线演示页面由前端静态数据驱动，不会连接真实数据库，适合快速了解页面结构和主要业务流程。若演示地址暂时无法访问，需要先在 GitHub 仓库中启用 `main /docs` 的 Pages 发布设置。

## 后续计划

下一步将重点完善真实部署与运维能力，包括生产环境配置、备份恢复、日志监控、真实 3D 模型性能优化，以及基于实验数据的智能分析和预警能力。也会继续根据实际实验室工作流程，细化审批规则、统计指标和移动端体验。

如果你正在做实验室数字化、管理系统开发、Vue 与 .NET 前后端分离实践，或者对业务数据与 3D 空间结合感兴趣，欢迎体验项目、提交 Issue，也欢迎在 GitHub 点一个 Star。
